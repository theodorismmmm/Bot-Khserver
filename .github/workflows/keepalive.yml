name: Keep Render App Awake

on:
  schedule:
    - cron: '*/12 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Open bot-khserver URL
        run: curl -fsS https://bot-khserver.onrender.com/
